import React from 'react';

const MultipleChoiceQuestion = (props) => {
  return (
    <div>
      <h1>{props.title}</h1>
      <p>This is a default component that can be imported into any page.</p>
    </div>
  );
}

export default MultipleChoiceQuestion;